const ctx=getCommitteeContext();
if(ctx){const {s,committee}=ctx;let db=getDB();markActive();function render(){const rows=(db.subcommitteeExpenses||[]).filter(x=>x.committeeId===committee.id).sort((a,b)=>new Date(b.date||b.createdAt)-new Date(a.date||a.createdAt));document.getElementById("page-content").innerHTML=`${pageTitle(`${escapeHTML(committee.name)} Expenses`,"",`<button id="add" class="btn btn-primary"><i class="bi bi-plus-circle me-2"></i>Add Expense</button>`)}<div class="row g-3 mb-4">${card("bi-wallet2","Office Allocation",subCommitteeAllocationTotal(committee.id,db))}${card("bi-receipt","Total Expenses",subCommitteeExpenseTotal(committee.id,db))}${card("bi-hourglass-split","Balance",subCommitteeBalance(committee.id,db))}</div><div class="panel mb-4" id="formWrap" style="display:none"><div class="panel-title mb-3">Add / Edit Expense</div><form id="form"><input type="hidden" id="editId"><div class="row g-3"><div class="col-md-4"><label class="form-label">Expense Details *</label><input id="title" class="form-control" required placeholder="What was the expense for?"></div><div class="col-md-4"><label class="form-label">Amount *</label><input id="amount" type="number" min="1" class="form-control" required></div><div class="col-md-4"><label class="form-label">Date *</label><input id="date" type="date" class="form-control" value="${new Date().toISOString().slice(0,10)}" required></div><div class="col-md-6"><label class="form-label">Remarks</label><textarea id="remarks" class="form-control" rows="2"></textarea></div><div class="col-md-6"><label class="form-label">Upload Bill</label><input id="bill" type="file" class="form-control" accept="image/*,.pdf"></div></div><div id="err" class="alert alert-danger d-none mt-3"></div><div class="d-flex justify-content-end gap-2 mt-3"><button type="button" id="cancel" class="btn btn-light">Cancel</button><button class="btn btn-primary">Save Expense</button></div></form></div><div class="panel"><div class="d-flex justify-content-between align-items-center mb-3"><div class="panel-title">Expense Records</div><button id="export" class="btn btn-sm btn-light"><i class="bi bi-file-earmark-spreadsheet me-1"></i>Excel / CSV</button></div>${rows.length?`<div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Details</th><th>Amount</th><th>Bill</th><th>Remarks</th><th>Actions</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${new Date(x.date||x.createdAt).toLocaleDateString("en-IN")}</td><td>${escapeHTML(x.title)}</td><td class="fw-semibold">${money(x.amount)}</td><td>${x.billData?`<a href="${x.billData}" target="_blank" class="btn btn-sm btn-light">View</a>`:"-"}</td><td>${escapeHTML(x.remarks||"-")}</td><td><div class="d-flex gap-1"><button class="btn btn-sm btn-light edit" data-id="${x.id}"><i class="bi bi-pencil"></i></button><button class="btn btn-sm btn-outline-danger del" data-id="${x.id}"><i class="bi bi-trash"></i></button></div></td></tr>`).join("")}</tbody></table></div>`:`<div class="empty-state">No expenses recorded.</div>`}</div>`;function card(i,l,v){return `<div class="col-6 col-xl-3"><div class="stat-card"><div class="stat-icon"><i class="bi ${i}"></i></div><div class="stat-label">${l}</div><div class="stat-value">${money(v)}</div></div></div>`}document.getElementById("add").onclick=()=>{document.getElementById("formWrap").style.display="block";document.getElementById("form").reset();document.getElementById("editId").value="";document.getElementById("date").value=new Date().toISOString().slice(0,10)};document.getElementById("cancel").onclick=()=>document.getElementById("formWrap").style.display="none";document.getElementById("form").onsubmit=e=>{e.preventDefault();const amount=Number(document.getElementById("amount").value),err=document.getElementById("err"),editId=document.getElementById("editId").value;if(amount<=0){err.textContent="Enter a valid amount.";err.classList.remove("d-none");return}const save=(billData)=>{const x={id:editId||uid("sce"),committeeId:committee.id,title:document.getElementById("title").value.trim(),amount,date:document.getElementById("date").value,remarks:document.getElementById("remarks").value.trim(),billData:billData||"",recordedBy:actorLabel(),createdAt:new Date().toISOString()};if(editId){const i=db.subcommitteeExpenses.findIndex(y=>y.id===editId);const old=db.subcommitteeExpenses[i];if(!billData)x.billData=old.billData||"";db.subcommitteeExpenses[i]=x;addActivity(db,{action:"Sub Committee Expense Edited",entityType:"subcommitteeExpense",entityId:x.id,summary:`${committee.name} expense edited`,oldValue:old,newValue:x})}else{db.subcommitteeExpenses.push(x);addActivity(db,{action:"Sub Committee Expense Added",entityType:"subcommitteeExpense",entityId:x.id,summary:`${committee.name} expense ${money(amount)}`,newValue:x})}saveDB(db);render()};const file=document.getElementById("bill").files[0];if(file){const reader=new FileReader();reader.onload=()=>save(reader.result);reader.readAsDataURL(file)}else save("")};document.querySelectorAll(".edit").forEach(b=>b.onclick=()=>{const x=db.subcommitteeExpenses.find(y=>y.id===b.dataset.id);document.getElementById("formWrap").style.display="block";document.getElementById("editId").value=x.id;document.getElementById("title").value=x.title;document.getElementById("amount").value=x.amount;document.getElementById("date").value=String(x.date||"").slice(0,10);document.getElementById("remarks").value=x.remarks||""});document.querySelectorAll(".del").forEach(b=>b.onclick=()=>{const x=db.subcommitteeExpenses.find(y=>y.id===b.dataset.id);if(!x||!confirm("Delete this expense?"))return;db.subcommitteeExpenses=db.subcommitteeExpenses.filter(y=>y.id!==x.id);addActivity(db,{action:"Sub Committee Expense Deleted",entityType:"subcommitteeExpense",entityId:x.id,summary:`${committee.name} expense deleted`,oldValue:x});saveDB(db);render()});document.getElementById("export").onclick=()=>exportCSV(rows.map(x=>({Date:x.date,Committee:committee.name,Details:x.title,Amount:x.amount,Remarks:x.remarks||"",Bill:x.billData?"Uploaded":""})),`${committee.id}-expenses.csv`)}render()}
