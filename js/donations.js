const db=getDB(),s=currentSession();markActive();
const visibleMembers=s.role==="admin"?db.members:db.members.filter(m=>Number(m.pradeshikamId)===Number(s.pradeshikamId));
document.getElementById("page-content").innerHTML=`
${pageTitle("Donations","",`<button id="exportDonations" class="btn btn-outline-primary"><i class="bi bi-download me-1"></i>CSV</button>`)}
<div class="row g-3 mb-4"><div class="col-md-4"><div class="stat-card"><div class="stat-label">Total Donations</div><div class="stat-value" id="donationTotal">₹0</div></div></div><div class="col-md-4"><div class="stat-card"><div class="stat-label">Member Donations</div><div class="stat-value" id="memberDonationTotal">₹0</div></div></div><div class="col-md-4"><div class="stat-card"><div class="stat-label">Other Donations</div><div class="stat-value" id="otherDonationTotal">₹0</div></div></div></div>
<div class="panel mb-4"><div class="panel-title mb-3">Add Donation</div><form id="donationForm" novalidate><div class="row g-3">
<div class="col-md-4"><label class="form-label">Donation Source *</label><select id="sourceType" class="form-select" required><option value="Member">Member</option><option value="Shop">Shop</option><option value="Organization">Organization</option><option value="Other">Other</option></select></div>
<div class="col-md-4" id="memberField"><label class="form-label">Member *</label><div class="member-picker"><input id="memberSearch" class="form-control" placeholder="Search name, phone or house number" autocomplete="off"><input id="donorMember" type="hidden"><div id="memberResults" class="member-results d-none"></div><div id="selectedMember" class="selected-member d-none"></div></div></div>
<div class="col-md-4" id="nameField"><label class="form-label">Donor / Organization Name *</label><input id="donorName" class="form-control"></div>
<div class="col-md-4"><label class="form-label">Pradeshikam *</label>${s.role==="admin"?`<select id="donationPradeshikam" class="form-select" required><option value="">Select Pradeshikam</option>${db.pradeshikams.map(p=>`<option value="${p.id}">${escapeHTML(p.name)}</option>`).join("")}</select>`:`<input id="donationPradeshikam" class="form-control" value="${escapeHTML(db.pradeshikams.find(p=>p.id===s.pradeshikamId)?.name||"")}" disabled>`}</div>
<div class="col-md-4"><label class="form-label">Amount *</label><input id="donationAmount" type="number" min="1" step="1" class="form-control" required></div><div class="col-md-4"><label class="form-label">Receipt Number *</label><input id="donationReceipt" class="form-control" required></div>
<div class="col-md-4"><label class="form-label">Payment Mode *</label><select id="donationMode" class="form-select" required><option>Cash</option><option>UPI</option><option>Bank</option><option>Check</option></select></div><div class="col-md-4"><label class="form-label">Date *</label><input id="donationDate" type="date" class="form-control" required></div><div class="col-md-4"><label class="form-label">Remarks</label><input id="donationRemarks" class="form-control"></div>
</div><div id="donationError" class="alert alert-danger d-none mt-3"></div><div class="d-flex justify-content-end mt-4"><button class="btn btn-primary"><i class="bi bi-gift me-1"></i>Save Donation</button></div></form></div>
<div class="panel"><div class="row g-2 mb-3"><div class="col-md-6"><input id="searchDonation" class="form-control" placeholder="Search donor, house, receipt or Pradeshikam"></div>${s.role==="admin"?`<div class="col-md-4"><select id="filterPr" class="form-select"><option value="">All Pradeshikams</option>${db.pradeshikams.map(p=>`<option value="${p.id}">${escapeHTML(p.name)}</option>`).join("")}</select></div>`:""}<div class="col-md-2"><select id="filterSource" class="form-select"><option value="">All sources</option><option>Member</option><option>Shop</option><option>Organization</option><option>Other</option></select></div></div><div id="donationTable"></div></div>`;
function todayValue(){const d=new Date();return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,10)}document.getElementById("donationDate").value=todayValue();
function updateSourceFields(){const type=document.getElementById("sourceType").value,isMember=type==="Member";document.getElementById("memberField").style.display=isMember?"block":"none";document.getElementById("nameField").style.display=isMember?"none":"block";document.getElementById("donorMember").required=isMember;document.getElementById("donorName").required=!isMember;if(!isMember){document.getElementById("memberSearch").value="";document.getElementById("memberResults").classList.add("d-none");document.getElementById("selectedMember").classList.add("d-none");document.getElementById("donorMember").value=""}}
function allDonations(){return (db.donations||[]).filter(d=>s.role==="admin"||Number(d.pradeshikamId)===Number(s.pradeshikamId))}
function donationView(d){const donor=d.donorMemberId?db.members.find(m=>m.id===d.donorMemberId):null;const pr=db.pradeshikams.find(p=>Number(p.id)===Number(d.pradeshikamId));return {d,donor,pr,name:d.donorName||donor?.name||d.sourceLabel||"-",house:d.houseNumber||donor?.houseNumber||"-"}}
function renderMemberResults(){const q=document.getElementById("memberSearch").value.trim().toLowerCase();const box=document.getElementById("memberResults");const arr=visibleMembers.filter(m=>!q||[m.name,m.phone,m.houseNumber,m.memberCode,m.countryCode].join(" ").toLowerCase().includes(q)).slice(0,30);box.innerHTML=!arr.length?`<div class="member-result-empty">No matching member</div>`:arr.map(m=>`<button type="button" class="member-result" data-id="${escapeHTML(m.id)}"><span><b>${escapeHTML(m.name)}</b><small>${escapeHTML(formatPhone(m.phone,m.countryCode))} · House ${escapeHTML(m.houseNumber||"-")}</small></span><i class="bi bi-chevron-right"></i></button>`).join("");box.classList.remove("d-none");box.querySelectorAll(".member-result").forEach(btn=>btn.addEventListener("click",()=>selectMember(btn.dataset.id)))}
function selectMember(id){const m=visibleMembers.find(x=>x.id===id);if(!m)return;document.getElementById("donorMember").value=m.id;document.getElementById("memberSearch").value="";document.getElementById("memberResults").classList.add("d-none");const sel=document.getElementById("selectedMember");sel.innerHTML=`<span><b>${escapeHTML(m.name)}</b><small>${escapeHTML(formatPhone(m.phone,m.countryCode))} · House ${escapeHTML(m.houseNumber||"-")}</small></span><button type="button" class="btn btn-sm btn-light" id="clearMember">Change</button>`;sel.classList.remove("d-none");if(s.role==="admin")document.getElementById("donationPradeshikam").value=m.pradeshikamId;document.getElementById("clearMember").addEventListener("click",()=>{document.getElementById("donorMember").value="";sel.classList.add("d-none");document.getElementById("memberSearch").focus()})}
function render(){const q=(document.getElementById("searchDonation").value||"").toLowerCase(),pid=document.getElementById("filterPr")?.value||"",src=document.getElementById("filterSource").value;const rows=allDonations().map(donationView).filter(x=>!pid||Number(x.d.pradeshikamId)===Number(pid)).filter(x=>!src||x.d.sourceType===src).filter(x=>{const text=[x.name,x.house,x.d.receiptNumber,x.pr?.name,x.d.paymentMode].join(" ").toLowerCase();return !q||text.includes(q)}).sort((a,b)=>new Date(b.d.date||b.d.createdAt)-new Date(a.d.date||a.d.createdAt));const total=rows.reduce((a,x)=>a+Number(x.d.amount||0),0),memberTotal=rows.filter(x=>x.d.sourceType==="Member").reduce((a,x)=>a+Number(x.d.amount||0),0);document.getElementById("donationTotal").textContent=money(total);document.getElementById("memberDonationTotal").textContent=money(memberTotal);document.getElementById("otherDonationTotal").textContent=money(total-memberTotal);document.getElementById("donationTable").innerHTML=!rows.length?`<div class="empty-state"><i class="bi bi-gift"></i>No donations found.</div>`:`<div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Pradeshikam</th><th>Source</th><th>Donor</th><th>House</th><th>Receipt</th><th>Mode</th><th>Amount</th><th>Actions</th></tr></thead><tbody>${rows.map(x=>`<tr><td data-label="Date">${new Date(x.d.date||x.d.createdAt).toLocaleDateString("en-IN")}</td><td data-label="Pradeshikam">${escapeHTML(x.pr?.name||"-")}</td><td data-label="Source">${escapeHTML(x.d.sourceType||"Member")}</td><td data-label="Donor">${escapeHTML(x.name)}</td><td data-label="House">${escapeHTML(x.house)}</td><td data-label="Receipt"><b>${escapeHTML(x.d.receiptNumber||x.d.reference||"-")}</b></td><td data-label="Mode">${escapeHTML(x.d.paymentMode||"-")}</td><td data-label="Amount" class="fw-semibold">${money(x.d.amount)}</td><td data-label="Actions"><div class="d-flex gap-1">${s.role==="admin"?`<a class="btn btn-sm btn-light" href="edit-donation.html?id=${encodeURIComponent(x.d.id)}" title="Edit"><i class="bi bi-pencil"></i></a>`:""}<button class="btn btn-sm btn-outline-danger delete-donation" data-id="${escapeHTML(x.d.id)}" title="Delete"><i class="bi bi-trash"></i></button></div></td></tr>`).join("")}</tbody></table></div>`;document.querySelectorAll(".delete-donation").forEach(btn=>btn.addEventListener("click",()=>deleteDonation(btn.dataset.id)))}
function deleteDonation(id){const d=db.donations.find(x=>x.id===id);if(!d)return;if(s.role!=="admin"&&Number(d.pradeshikamId)!==Number(s.pradeshikamId))return;if(!confirm(`Delete donation receipt ${d.receiptNumber} for ${money(d.amount)}?`))return;addActivity(db,{action:"Donation Deleted",entityType:"donation",entityId:d.id,memberId:d.donorMemberId||null,pradeshikamId:d.pradeshikamId,summary:`Donation ${d.receiptNumber} deleted`,details:`${money(d.amount)} donation from ${d.donorName||"donor"}.`,oldValue:donationSnapshot(d)});db.donations=db.donations.filter(x=>x.id!==id);saveDB(db);render()}
document.getElementById("sourceType").addEventListener("change",updateSourceFields);document.getElementById("memberSearch").addEventListener("input",renderMemberResults);document.getElementById("memberSearch").addEventListener("focus",renderMemberResults);document.getElementById("searchDonation").addEventListener("input",render);document.getElementById("filterSource").addEventListener("change",render);document.getElementById("filterPr")?.addEventListener("change",render);document.addEventListener("click",e=>{if(!e.target.closest(".member-picker"))document.getElementById("memberResults")?.classList.add("d-none")});
document.getElementById("donationForm").addEventListener("submit",e=>{e.preventDefault();const form=e.currentTarget,err=document.getElementById("donationError");err.classList.add("d-none");let ok=true;form.querySelectorAll("[required]").forEach(el=>{const empty=!String(el.value||"").trim();el.classList.toggle("is-invalid",empty);if(empty)ok=false});if(!ok){err.textContent="Please fill in all required fields.";err.classList.remove("d-none");return}const type=document.getElementById("sourceType").value,memberId=document.getElementById("donorMember").value,name=document.getElementById("donorName").value.trim(),amount=Number(document.getElementById("donationAmount").value),receipt=document.getElementById("donationReceipt").value.trim(),mode=document.getElementById("donationMode").value,date=document.getElementById("donationDate").value,remarks=document.getElementById("donationRemarks").value.trim(),donor=memberId?db.members.find(m=>m.id===memberId):null,pradeshikamId=donor?donor.pradeshikamId:(s.role==="admin"?Number(document.getElementById("donationPradeshikam").value||0):Number(s.pradeshikamId));if(type==="Member"&&!donor){err.textContent="Select a member from the search results.";err.classList.remove("d-none");return}if(amount<=0){err.textContent="Enter a valid donation amount.";err.classList.remove("d-none");return}if(!pradeshikamId){err.textContent="Select a Pradeshikam.";err.classList.remove("d-none");return}const receiptUsed=[...db.payments,...(db.donations||[])].some(x=>String(x.receiptNumber||"").toLowerCase()===receipt.toLowerCase());if(receiptUsed){err.textContent="This receipt number is already in use.";err.classList.remove("d-none");return}const donation={id:uid("don"),donorMemberId:donor?.id||null,donorName:donor?.name||name,pradeshikamId,houseNumber:donor?.houseNumber||"",amount,receiptNumber:receipt,sourceType:type,sourceLabel:type,paymentMode:mode,date:new Date(date+"T12:00:00").toISOString(),remarks,createdAt:new Date().toISOString()};db.donations.push(donation);addActivity(db,{action:"Donation Added",entityType:"donation",entityId:donation.id,memberId:donor?.id||null,pradeshikamId,summary:`${money(amount)} donation recorded`,details:`${type} donation from ${donation.donorName} with receipt ${receipt}.`,newValue:donationSnapshot(donation)});saveDB(db);form.reset();document.getElementById("donationDate").value=todayValue();updateSourceFields();render()});
document.getElementById("exportDonations").addEventListener("click",()=>{const rows=allDonations().map(d=>{const donor=d.donorMemberId?db.members.find(m=>m.id===d.donorMemberId):null,pr=db.pradeshikams.find(p=>Number(p.id)===Number(d.pradeshikamId));return {Date:new Date(d.date||d.createdAt).toLocaleString("en-IN"),Pradeshikam:pr?.name||"",Source:d.sourceType||"Member",Donor:d.donorName||donor?.name||"",House:d.houseNumber||donor?.houseNumber||"",Receipt:d.receiptNumber||"",Amount:d.amount,PaymentMode:d.paymentMode||"",Remarks:d.remarks||""}});exportCSV(rows,"fcms-donations.csv")});render();
