const db=getDB(),s=currentSession();markActive();
if(s.role!=="admin"){location.href="donations.html"}
const id=new URLSearchParams(location.search).get("id"),donation=db.donations.find(d=>d.id===id);
if(!donation){location.href="donations.html"}else{
const donor=donation.donorMemberId?db.members.find(m=>m.id===donation.donorMemberId):null;
document.getElementById("page-content").innerHTML=`${pageTitle("Edit Donation")}<div class="panel form-card"><form id="editDonationForm"><div class="row g-3"><div class="col-md-4"><label class="form-label">Source *</label><select id="source" class="form-select" required>${["Member","Shop","Organization","Other"].map(x=>`<option ${donation.sourceType===x?"selected":""}>${x}</option>`).join("")}</select></div><div class="col-md-4"><label class="form-label">Donor Name *</label><input id="name" class="form-control" required value="${escapeHTML(donation.donorName||donor?.name||"")}"></div><div class="col-md-4"><label class="form-label">Pradeshikam *</label><select id="pradeshikam" class="form-select" required>${db.pradeshikams.map(p=>`<option value="${p.id}" ${Number(donation.pradeshikamId)===Number(p.id)?"selected":""}>${escapeHTML(p.name)}</option>`).join("")}</select></div><div class="col-md-4"><label class="form-label">Amount *</label><input id="amount" type="number" min="1" class="form-control" required value="${Number(donation.amount)}"></div><div class="col-md-4"><label class="form-label">Receipt Number *</label><input id="receipt" class="form-control" required value="${escapeHTML(donation.receiptNumber||"")}"></div><div class="col-md-4"><label class="form-label">Payment Mode *</label><select id="mode" class="form-select" required>${["Cash","UPI","Bank","Check"].map(x=>`<option ${donation.paymentMode===x?"selected":""}>${x}</option>`).join("")}</select></div><div class="col-md-6"><label class="form-label">Date *</label><input id="date" type="date" class="form-control" required value="${new Date(donation.date||donation.createdAt).toISOString().slice(0,10)}"></div><div class="col-md-6"><label class="form-label">Remarks</label><input id="remarks" class="form-control" value="${escapeHTML(donation.remarks||"")}"></div></div><div id="formError" class="alert alert-danger d-none mt-3"></div><div class="d-flex justify-content-end gap-2 mt-4"><a href="donations.html" class="btn btn-light">Cancel</a><button class="btn btn-primary">Save Changes</button></div></form></div>`;
document.getElementById("editDonationForm").addEventListener("submit",e=>{e.preventDefault();const receipt=document.getElementById("receipt").value.trim(),amount=Number(document.getElementById("amount").value),err=document.getElementById("formError"),dup=[...db.payments,...(db.donations||[])].find(x=>x!==donation&&String(x.receiptNumber||"").toLowerCase()===receipt.toLowerCase());if(dup){err.textContent="That receipt number is already in use.";err.classList.remove("d-none");return}if(amount<=0){err.textContent="Enter a valid amount.";err.classList.remove("d-none");return}const old=donationSnapshot(donation);Object.assign(donation,{sourceType:document.getElementById("source").value,donorName:document.getElementById("name").value.trim(),pradeshikamId:Number(document.getElementById("pradeshikam").value),amount,receiptNumber:receipt,paymentMode:document.getElementById("mode").value,date:new Date(document.getElementById("date").value+"T12:00:00").toISOString(),remarks:document.getElementById("remarks").value.trim()});addActivity(db,{action:"Donation Edited",entityType:"donation",entityId:donation.id,memberId:donation.donorMemberId||null,pradeshikamId:donation.pradeshikamId,summary:`Donation ${receipt} edited`,details:"Donation details edited by Main Committee.",oldValue:old,newValue:donationSnapshot(donation)});saveDB(db);location.href="donations.html"})}
